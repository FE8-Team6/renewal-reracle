const LoginToSignUpTitle = ({ title }: { title: string }) => {
  return (
    <div className="w-full h-[2rem] top-0 bg-green text-[1.8vh] font-bold text-purple flex justify-center items-center">
      {title}
    </div>
  );
};

export default LoginToSignUpTitle;
