const LoginToSignUpTitle = ({ title }: { title: string }) => {
  return (
    <div className="w-full h-[2rem] text-center bg-green text-[2vh] font-bold text-purple flex items-center justify-center">
      {title}
    </div>
  );
};

export default LoginToSignUpTitle;
